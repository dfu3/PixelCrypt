__author__ = 'dfu3'


#--------------------------------------------------------------------
        # def chunkafy(list, range):
        #     for i in xrange(0, len(list), range):
        #         yield list[i:i + 1]
        #
        # randR = random.randint(3, 9)
        #
        # padLen = len(encryptMess) % randR
        # padLen = randR - padLen
        #
        # bitPad = ""
        # for bit in range(padLen - 1):
        #     bitPad += "0"
        # bitPad += "1"
        #
        # encryptMess = bitPad + encryptMess
        #
        # newMess = list( textwrap.wrap(encryptMess, randR) )
        # newBits = ""
        #
        #
        # decList =[]
        #
        # for chunk in newMess:
        #     decNum = 0
        #     for seq in chunk:
        #
        #         decNum *= 2
        #
        #         if(seq == "1"):
        #             decNum += 1
        #
        #     decList.append(decNum)
        #-------------------------------------------------
        # randx1 = random.randint(0, 1000000)
        # randy1 = random.randint(0, 1000000)
        #
        # randx2 = random.randint(0, 1000000)
        # randy2 = random.randint(0, 1000000)
        #
        # randx3 = random.randint(0, 1000000)
        # randy3 = random.randint(0, 1000000)
        #
        # keyList = [randx1, randy1, randx2, randy2, randx3, randy3]
        #
        # for i in range(0, len(keyList), 2):
        #     img = img.offset(keyList[i], keyList[i + 1])
        #
        # key = [0, 0, 0, 0, 0, 0]
        # count = 1
        #
        # for i in keyList:
        #     key[len(keyList) - count] = i
        #     count +=
        # password = ""
        # returnVals = [key, password]


        #--------------------------------------------------------------------

         # for i in range(0, len(keyList), 2):
        #     img = img.offset(-(keyList[i + 1]), -(keyList[i]))